syntax = "proto3";
package lists;

import "schema/ids/base/anyID.proto";
import "gogoproto/gogo.proto";

message IDList {
  option (gogoproto.goproto_getters) = false;

  repeated ids.AnyID i_d_list = 1;
}
